import { prisma } from "../../src/util/database";

describe("create", () => {
  test.todo("should create auction");
});

describe("get", () => {
  test.todo("should get all auction");
});

describe("update", () => {
  test.todo("should update auction");
});
