{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "monacute-oss",
  "private": "true",
  "license": "See README.md",
  "author": "Ryota Uno <palon7@gmail.com>",
  "description": "Monorepo for monacute",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:task": "yarn workspace @palon7/monacute-task build",
    "prisma:generate": "yarn workspace @palon7/monacute-task prisma generate",
    "prisma:migrate": "yarn workspace @palon7/monacute-task prisma migrate deploy"
  }
}
